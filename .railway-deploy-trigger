# Railway Deploy Trigger - DRAAD108-DEPLOY

Timestamp: 2025-12-05T20:10:15+01:00
DRAAD: 108-DEPLOY
Reason: Force rebuild after railway.json Dockerfile fix
Random trigger: 87392

## PROBLEEM:

Railway.json forceerde NIXPACKS builder:
```json
{
  "build": {
    "builder": "NIXPACKS"  // FOUT - zoekt Dockerfile in root
  }
}
```

Error: "failed to read Dockerfile at 'Dockerfile'"
Oorzaak: Dockerfile staat in solver/docker/Dockerfile, niet in root

## OPLOSSING:

1. railway.json aangepast naar:
```json
{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "solver/docker/Dockerfile"
  }
}
```

2. Root Directory in Railway: /solver (al ingesteld)

3. Deze cache-bust forceert nieuwe deploy

## COMMITS:

- 39098188: railway.json fix - DOCKERFILE builder
- Dit bestand: Force Railway rebuild trigger

## VERWACHT RESULTAAT:

✅ Railway vindt Dockerfile op solver/docker/Dockerfile
✅ Build succeeds met Python 3.11 + OR-Tools
✅ Solver service draait met DRAAD108 code
✅ /version endpoint geeft 1.1.0-DRAAD108
