# DRAAD 195: Requirements for GREEDY Separate Railway Service
# Minimal, aligned dependencies for GREEDY CP-SAT solver engine
# FIXED: postgrest-py removed (doesn't exist), aligned with baseline services

# Web Framework (aligned with baseline services)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.8.0
pydantic-settings==2.1.0
python-multipart==0.0.18

# Database (aligned with rooster-app-verloskunde baseline)
supabase==2.4.0
# NOTE: postgrest-py, realtime-py, postgres-py, python-gotrue are handled by supabase
# postgrest-py==0.13.0 doesn't exist on PyPI (max available: 0.10.6)
# Using supabase==2.4.0 which includes compatible versions internally

# HTTP Client (aligned with baseline)
httpx==0.25.2
requests==2.32.4

# Utilities
python-dotenv==1.0.0
pytz==2023.3

# Logging
python-json-logger==2.0.7

# Development (optional, but included for consistency with main service)
black==23.12.0
flake8==6.1.0
pytest==7.4.3
pytest-asyncio==0.21.1

# Monitoring
prometheus-client==0.19.0
