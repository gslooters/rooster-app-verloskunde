[build]
# Build command - Railway injects env vars BEFORE running this
command = "npm ci && npm run build"

[deploy]
# Standalone server path
startCommand = "node .next/standalone/server.js"

# Health check
healthcheckPath = "/api/health"
healthcheckTimeout = 100

# Restart policy
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# DRAAD186: Railway environment variables injected at build time
# Date: 2025-12-15T20:04:00Z
# CRITICAL: Ensure these environment variables are set in Railway:
#   - NEXT_PUBLIC_SUPABASE_URL
#   - NEXT_PUBLIC_SUPABASE_ANON_KEY
# Cache-bust: 1734278674000
