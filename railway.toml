[build]
# Clean build command
command = "npm ci && npm run build"

[deploy]
# Correct standalone server path
startCommand = "HOSTNAME=0.0.0.0 PORT=$PORT node .next/standalone/server.js"

# Health check
healthcheckPath = "/api/health"
healthcheckTimeout = 100

# Restart policy
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Cache-busting deployment trigger: 1734283963
# DRAAD182: Fixed AutoFillButton.tsx TypeScript syntax error (removed invalid docstring)
# Force rebuild and deployment on Railway
# Updated: 2025-12-15T14:46:03Z
