[build]
# Clean build command
command = "npm ci && npm run build"

[deploy]
# Correct standalone server path
startCommand = "HOSTNAME=0.0.0.0 PORT=$PORT node .next/standalone/server.js"

# Health check
healthcheckPath = "/api/health"
healthcheckTimeout = 100

# Restart policy
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Cache-busting deployment trigger: 1734285215
# DRAAD185: Fixed Python 3.13 incompatibility issues
# - Main app: pydantic 2.5.0 → 2.8.0
# - Solver: pydantic 2.10.5 → 2.8.0
# - Updated typing-extensions 4.8.0 → 4.9.0
# Force rebuild and deployment on Railway
# Updated: 2025-12-15T17:34:00Z
