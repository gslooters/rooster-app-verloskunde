[build]
# Clean build command
command = "npm ci && npm run build"

[deploy]
# Correct standalone server path
startCommand = "HOSTNAME=0.0.0.0 PORT=$PORT node .next/standalone/server.js"

# Health check
healthcheckPath = "/api/health"
healthcheckTimeout = 100

# Restart policy
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Cache-busting deployment trigger: 1734283800
# DRAAD176: Fixed Node version resolution by removing invalid nixpacks line
# Railway auto-detects Node 20.x from package.json engines field
