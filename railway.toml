[build]
# Force clean build - geen cache
command = "npm install && npm run build"

[deploy]
# 🔥 CRITICAL FIX: Correct standalone server path
# Gebruik HOSTNAME=0.0.0.0 voor Railway networking
startCommand = "HOSTNAME=0.0.0.0 PORT=$PORT node .next/standalone/server.js"

# Health check
healthcheckPath = "/api/health"
healthcheckTimeout = 100

# Restart policy
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Deployment trigger: 1733998080
